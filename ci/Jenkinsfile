pipeline {
    agent any
    
    parameters {
        choice(
            name: 'ENVIRONMENT',
            choices: ['dev', 'staging', 'production'],
            description: 'Which environment to deploy to?'
        )

        booleanParam(
            name: 'RUN_TESTS',
            defaultValue: false,
            description: 'Run tests? (Adds ~3s)'
        )
    }

    environment {
        DOCKER_REGISTRY     =   credentials('docker-registry-url')
        DOCKER_IMAGE_PREFIX =   credentials('docker-image-prefix')
        GIT_COMMIT_SHORT    =   sh(script: "git rev-parse --short HEAD", returnStdout: true).trim()
        IMAGE_TAG           =   "${BUILD_NUMBER}-${GIT_COMMIT_SHORT}"
        DOCKER_BUILDKIT     =   "1"
        DEPLOY_ENV          =   "${params.ENVIRONMENT}"
    }
    
    stages {
        stage('Checkout') {
            steps {
                echo '📦 Checking out code...'
                checkout scm
            }
        }
        
        stage('Test') {
            when {
                expression { params.RUN_TESTS == true }
            }

            steps {
                echo '🧪 Running tests in Python container...'
                sh '''
                    docker run --rm \
                        -v $WORKSPACE:/app \
                        -w /app \
                        python:3.11-slim \
                        bash -c "
                            pip install -r build/requirements.txt || true
                            python -m pytest tests/ || true  
                            flake8 src/ || true
                        " || echo "Tests completed"
                '''
            }
        }
        
        stage('Build Docker Images') {
            environment {
                // Define these locally to avoid interpolation warnings
                REGISTRY = "${env.DOCKER_REGISTRY}"
                PREFIX = "${env.DOCKER_IMAGE_PREFIX}"
                TAG = "${env.IMAGE_TAG}"
            }
            steps {
                echo '🐳 Building multi-stage Docker images in parallel...'
                script {
                    echo "Building images with tag: ${TAG}"
                    
                    parallel(
                        'data-fetcher': {
                            echo '📦 Building Data Fetcher...'
                            sh '''
                                docker build \
                                    --target data-fetcher \
                                    -f build/Dockerfile.dev \
                                    -t ${REGISTRY}/${PREFIX}/dota2-fetcher:${TAG} \
                                    .
                            '''
                        },
                        'trainer': {
                            echo '🤖 Building ML Trainer...'
                            sh '''
                                docker build \
                                    --target trainer \
                                    -f build/Dockerfile.dev \
                                    -t ${REGISTRY}/${PREFIX}/dota2-trainer:${TAG} \
                                    .
                            '''
                        },
                        'api': {
                            echo '🌐 Building API Service...'
                            sh '''
                                docker build \
                                    --target api \
                                    -f build/Dockerfile.dev \
                                    -t ${REGISTRY}/${PREFIX}/dota2-api:${TAG} \
                                    .
                            '''
                        }
                    )
                }
            }
        }
        
        stage('Push Docker Images') {
            steps {
                echo '📤 Pushing images to Docker Hub...'
                script {
                    // Determine if we should push :latest tag
                    def pushLatest = (params.ENVIRONMENT == 'production')
                    
                    parallel(
                        'push-fetcher': {
                            sh """
                                docker push ${DOCKER_REGISTRY}/${DOCKER_IMAGE_PREFIX}/dota2-fetcher:${IMAGE_TAG}
                            """
                            // Only push :latest for production
                            if (pushLatest) {
                                sh """
                                    docker push ${DOCKER_REGISTRY}/${DOCKER_IMAGE_PREFIX}/dota2-fetcher:latest
                                """
                            }
                        },
                        'push-trainer': {
                            sh """
                                docker push ${DOCKER_REGISTRY}/${DOCKER_IMAGE_PREFIX}/dota2-trainer:${IMAGE_TAG}
                            """
                            if (pushLatest) {
                                sh """
                                    docker push ${DOCKER_REGISTRY}/${DOCKER_IMAGE_PREFIX}/dota2-trainer:latest
                                """
                            }
                        },
                        'push-api': {
                            sh """
                                docker push ${DOCKER_REGISTRY}/${DOCKER_IMAGE_PREFIX}/dota2-api:${IMAGE_TAG}
                            """
                            if (pushLatest) {
                                sh """
                                    docker push ${DOCKER_REGISTRY}/${DOCKER_IMAGE_PREFIX}/dota2-api:latest
                                """
                            }
                        }
                    )
                    
                    if (pushLatest) {
                        echo '📦 Pushed :latest tags for production'
                    } else {
                        echo '⏭️  Skipped :latest tags (not production)'
                    }
                }
            }
        }

        stage('Set Repository Privacy') {
            steps {
                sh '''
                    # Function to make repo private
                    make_private() {
                        local repo=$1
                        
                        # Get token
                        TOKEN=$(curl -s -H "Content-Type: application/json" \
                        -X POST \
                        -d '{"username": "'${DOCKERHUB_USERNAME}'", "password": "'${DOCKERHUB_TOKEN}'"}' \
                        https://hub.docker.com/v2/users/login/ | jq -r .token)
                        
                        # Make private
                        curl -s -H "Authorization: JWT ${TOKEN}" \
                        -H "Content-Type: application/json" \
                        -X PATCH \
                        -d '{"is_private": true}' \
                        https://hub.docker.com/v2/repositories/${DOCKERHUB_USERNAME}/${repo}/
                    }
                    
                    # Make specific repos private
                    make_private "dota2-fetcher"
                    # make_private "dota2-trainer"  # Uncomment if needed
                '''
            }
        }
        
        stage('Deploy to Environment') {
            steps {
                script {
                    echo "🚀 Deploying to ${DEPLOY_ENV}..."
                    
                    // Production requires approval
                    if (DEPLOY_ENV == 'production') {
                        input message: '⚠️  Deploy to PRODUCTION?', ok: 'Deploy'
                    }
                    
                    // Use your existing deploy-with-helm.sh script
                    sh """
                        cd cli
                        chmod +x deploy-with-helm.sh
                        ./deploy-with-helm.sh ${DEPLOY_ENV} ${IMAGE_TAG}
                    """
                }
            }
        }
        
        stage('Verify Deployment') {
            steps {
                echo '🔍 Verifying deployment...'
                sh """
                    kubectl get pods -n data
                    kubectl get svc -n data
                """
            }
        }

        
    }
    
    post {
        always {
            echo '🧹 Cleaning up...'
            script {
                sh """
                    docker rmi ${DOCKER_REGISTRY}/${DOCKER_IMAGE_PREFIX}/dota2-fetcher:${IMAGE_TAG} || true
                    docker rmi ${DOCKER_REGISTRY}/${DOCKER_IMAGE_PREFIX}/dota2-fetcher:latest || true
                    docker rmi ${DOCKER_REGISTRY}/${DOCKER_IMAGE_PREFIX}/dota2-trainer:${IMAGE_TAG} || true
                    docker rmi ${DOCKER_REGISTRY}/${DOCKER_IMAGE_PREFIX}/dota2-trainer:latest || true
                    docker rmi ${DOCKER_REGISTRY}/${DOCKER_IMAGE_PREFIX}/dota2-api:${IMAGE_TAG} || true
                    docker rmi ${DOCKER_REGISTRY}/${DOCKER_IMAGE_PREFIX}/dota2-api:latest || true
                """
            }
        }
        success {
            echo '✅ Pipeline completed successfully!'
            echo "🚀 Deployed ${IMAGE_TAG} to ${DEPLOY_ENV}"  // ← ADD ONLY THIS LINE
        }
        failure {
            echo '❌ Pipeline failed!'
        }
    }
}