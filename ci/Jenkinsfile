pipeline {
    agent any
    
    parameters {
        choice(
            name: 'ENVIRONMENT',
            choices: ['dev', 'staging', 'production'],
            description: 'Which environment to deploy to?'
        )
        booleanParam(
            name: 'RUN_TESTS',
            defaultValue: false,
            description: 'Run tests? (Adds ~3s)'
        )
    }

    environment {
        // Read from environment variables set by Kubernetes
        DOCKER_REGISTRY     = "${env.DOCKER_REGISTRY}"
        DOCKER_IMAGE_PREFIX = "${env.DOCKER_IMAGE_PREFIX}"
        GIT_COMMIT_SHORT    = sh(script: "git rev-parse --short HEAD", returnStdout: true).trim()
        IMAGE_TAG           = "${BUILD_NUMBER}-${GIT_COMMIT_SHORT}"
        DOCKER_BUILDKIT     = "1"
        SLACK_WEBHOOK       = "${env.SLACK_WEBHOOK_URL}"
    }
    
    stages {
        stage('Initialize') {
            steps {
                script {
                    // Use parameter if provided, otherwise auto-detect from branch
                    if (params.ENVIRONMENT) {
                        env.DEPLOY_ENV = params.ENVIRONMENT
                        echo "📝 Using parameter environment: ${params.ENVIRONMENT}"
                    } else {
                        env.DEPLOY_ENV = determineEnvironment()
                        echo "🔍 Auto-detected environment from branch: ${env.DEPLOY_ENV}"
                    }
                    
                    env.GIT_AUTHOR = sh(script: "git log -1 --pretty=format:'%an'", returnStdout: true).trim()
                    env.GIT_COMMIT_MSG = sh(script: "git log -1 --pretty=format:'%s'", returnStdout: true).trim()
                    
                    echo "================================"
                    echo "🔧 Build Configuration"
                    echo "================================"
                    echo "Environment: ${env.DEPLOY_ENV}"
                    echo "Image Tag: ${IMAGE_TAG}"
                    echo "Branch: ${env.GIT_BRANCH}"
                    echo "Author: ${env.GIT_AUTHOR}"
                    echo "================================"
                }
            }
        }

        stage('Notify Start') {
            steps {
                script {
                    sendSlackNotification(
                        "🚀 *Build Started* #${BUILD_NUMBER}",
                        "Branch: `${env.GIT_BRANCH}`\nEnvironment: `${env.DEPLOY_ENV}`\nCommit: `${GIT_COMMIT_SHORT}`\nAuthor: ${env.GIT_AUTHOR}",
                        "#36a64f"
                    )
                }
            }
        }

        stage('Checkout') {
            steps {
                echo '📦 Checking out code...'
                checkout scm
            }
        }
        
        stage('Test') {
            when {
                expression { 
                    params.RUN_TESTS == true || env.GIT_BRANCH == 'origin/main' 
                }
            }
            steps {
                echo '🧪 Running tests...'
                sh '''
                    docker run --rm \
                        -v $WORKSPACE:/app \
                        -w /app \
                        python:3.11-slim \
                        bash -c "
                            pip install -q -r build/requirements.txt || echo 'No requirements.txt'
                            python -m pytest tests/ -q || echo 'Tests not available'
                            flake8 src/ || echo 'Linting skipped'
                        " || echo "Tests completed"
                '''
            }
        }
        
        stage('Build Docker Images') {
            steps {
                echo '🐳 Building multi-stage Docker images in parallel...'
                script {
                    sendSlackNotification(
                        "🐳 Building Docker Images...",
                        "Tag: `${IMAGE_TAG}`",
                        "#439FE0"
                    )

                    parallel(
                        'data-fetcher': {
                            echo '📦 Building Data Fetcher...'
                            sh """
                                docker build \
                                    --target data-fetcher \
                                    -f build/Dockerfile.dev \
                                    -t ${DOCKER_REGISTRY}/${DOCKER_IMAGE_PREFIX}/dota2-fetcher:${IMAGE_TAG} \
                                    .
                            """
                        },
                        'trainer': {
                            echo '🤖 Building ML Trainer...'
                            sh """
                                docker build \
                                    --target trainer \
                                    -f build/Dockerfile.dev \
                                    -t ${DOCKER_REGISTRY}/${DOCKER_IMAGE_PREFIX}/dota2-trainer:${IMAGE_TAG} \
                                    .
                            """
                        },
                        'api': {
                            echo '🌐 Building API Service...'
                            sh """
                                docker build \
                                    --target api \
                                    -f build/Dockerfile.dev \
                                    -t ${DOCKER_REGISTRY}/${DOCKER_IMAGE_PREFIX}/dota2-api:${IMAGE_TAG} \
                                    .
                            """
                        }
                    )
                }
            }
        }
        
        stage('Push Docker Images') {
            steps {
                echo '📤 Pushing images to Docker Hub...'
                script {
                    def pushLatest = (env.DEPLOY_ENV == 'production')
                    
                    // Tag with :latest if production
                    if (pushLatest) {
                        sh """
                            docker tag ${DOCKER_REGISTRY}/${DOCKER_IMAGE_PREFIX}/dota2-fetcher:${IMAGE_TAG} \
                                ${DOCKER_REGISTRY}/${DOCKER_IMAGE_PREFIX}/dota2-fetcher:latest
                            docker tag ${DOCKER_REGISTRY}/${DOCKER_IMAGE_PREFIX}/dota2-trainer:${IMAGE_TAG} \
                                ${DOCKER_REGISTRY}/${DOCKER_IMAGE_PREFIX}/dota2-trainer:latest
                            docker tag ${DOCKER_REGISTRY}/${DOCKER_IMAGE_PREFIX}/dota2-api:${IMAGE_TAG} \
                                ${DOCKER_REGISTRY}/${DOCKER_IMAGE_PREFIX}/dota2-api:latest
                        """
                    }
                    
                    parallel(
                        'push-fetcher': {
                            sh """
                                docker push ${DOCKER_REGISTRY}/${DOCKER_IMAGE_PREFIX}/dota2-fetcher:${IMAGE_TAG}
                            """
                            if (pushLatest) {
                                sh """
                                    docker push ${DOCKER_REGISTRY}/${DOCKER_IMAGE_PREFIX}/dota2-fetcher:latest
                                """
                            }
                        },
                        'push-trainer': {
                            sh """
                                docker push ${DOCKER_REGISTRY}/${DOCKER_IMAGE_PREFIX}/dota2-trainer:${IMAGE_TAG}
                            """
                            if (pushLatest) {
                                sh """
                                    docker push ${DOCKER_REGISTRY}/${DOCKER_IMAGE_PREFIX}/dota2-trainer:latest
                                """
                            }
                        },
                        'push-api': {
                            sh """
                                docker push ${DOCKER_REGISTRY}/${DOCKER_IMAGE_PREFIX}/dota2-api:${IMAGE_TAG}
                            """
                            if (pushLatest) {
                                sh """
                                    docker push ${DOCKER_REGISTRY}/${DOCKER_IMAGE_PREFIX}/dota2-api:latest
                                """
                            }
                        }
                    )
                    
                    echo pushLatest ? '📦 Pushed versioned + :latest tags' : '📦 Pushed versioned tags only'
                }
            }
        }
        
        stage('Deploy to Environment') {
            steps {
                script {
                    echo "🚀 Deploying to ${env.DEPLOY_ENV}..."
                    
                    sendSlackNotification(
                        "🚀 Deploying to ${env.DEPLOY_ENV}...",
                        "Image: `${IMAGE_TAG}`",
                        "#FFA500"
                    )

                    if (env.DEPLOY_ENV == 'production') {
                        timeout(time: 30, unit: 'MINUTES') {
                            input message: '⚠️  Deploy to PRODUCTION?', ok: 'Deploy'
                        }
                    }
                    
                    sh """
                        cd cli
                        chmod +x deploy-with-helm.sh
                        ./deploy-with-helm.sh ${env.DEPLOY_ENV} ${IMAGE_TAG}
                    """
                }
            }
        }
        
        stage('Verify Deployment') {
            steps {
                echo '🔍 Verifying deployment...'
                sh """
                    kubectl get pods -n data
                    kubectl get svc -n data
                    
                    echo ""
                    echo "Waiting for pods to be ready..."
                    kubectl wait --for=condition=ready pod \
                        -l app=dota2-meta-lab \
                        -n data \
                        --timeout=300s || echo "⚠️  Some pods not ready yet"
                """
            }
        }
    }
    
    post {
        always {
            echo '🧹 Cleaning up...'
            sh """
                docker rmi ${DOCKER_REGISTRY}/${DOCKER_IMAGE_PREFIX}/dota2-fetcher:${IMAGE_TAG} || true
                docker rmi ${DOCKER_REGISTRY}/${DOCKER_IMAGE_PREFIX}/dota2-trainer:${IMAGE_TAG} || true
                docker rmi ${DOCKER_REGISTRY}/${DOCKER_IMAGE_PREFIX}/dota2-api:${IMAGE_TAG} || true
            """
        }
        
        success {
            script {
                def duration = currentBuild.durationString.replace(' and counting', '')
                
                echo '✅ Pipeline completed successfully!'
                
                sendSlackNotification(
                    "✅ *Deployment Successful!*",
                    """*Environment:* `${env.DEPLOY_ENV}`
*Build:* #${BUILD_NUMBER}
*Duration:* ${duration}
*Image:* `${IMAGE_TAG}`
*Author:* ${env.GIT_AUTHOR}
*Commit:* ${env.GIT_COMMIT_MSG}
*Branch:* `${env.GIT_BRANCH}`""",
                    "good"
                )
            }
        }
        
        failure {
            script {
                def duration = currentBuild.durationString.replace(' and counting', '')
                
                echo '❌ Pipeline failed!'
                
                sendSlackNotification(
                    "❌ *Deployment Failed!*",
                    """*Environment:* `${env.DEPLOY_ENV}`
*Build:* #${BUILD_NUMBER}
*Duration:* ${duration}
*Branch:* `${env.GIT_BRANCH}`
*Console:* ${env.BUILD_URL}console""",
                    "danger"
                )
            }
        }
    }
}

// Helper function to determine environment from branch
def determineEnvironment() {
    def branch = env.GIT_BRANCH ?: 'unknown'
    
    if (branch.contains('main')) {
        return 'production'
    } else if (branch.contains('staging')) {
        return 'staging'
    } else if (branch.contains('dev') || branch.contains('develop')) {
        return 'dev'
    } else {
        return 'dev'
    }
}

// Helper function to send Slack notifications
def sendSlackNotification(String title, String message, String color) {
    script {
        try {
            def payload = groovy.json.JsonOutput.toJson([
                attachments: [[
                    color: color,
                    title: title,
                    text: message,
                    footer: "Jenkins CI/CD",
                    footer_icon: "https://jenkins.io/images/logos/jenkins/jenkins.png",
                    ts: System.currentTimeMillis() / 1000
                ]]
            ])
            
            sh """
                curl -X POST '${SLACK_WEBHOOK}' \
                -H 'Content-Type: application/json' \
                -d '${payload}'
            """
        } catch (Exception e) {
            echo "Slack notification failed: ${e.message}"
        }
    }
}